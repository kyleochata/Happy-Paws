import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '95%',
  },
  webContainer: {
    width: '100%',
    maxWidth: 1280,
  }
});

export default styles;